<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>タスク一覧</h1>
    <%= link_to "新規タスク作成", new_task_path, class: "btn btn-primary" %>
  </div>

  <% if @tasks.any? %>
    <div class="list-group">
      <% @tasks.each do |task| %>
        <div class="list-group-item list-group-item-action">
          <div class="d-flex w-100 justify-content-between align-items-center">
            <div class="d-flex align-items-center">
              <%# ✅ 状態アイコン（表示のみ） %>
              <% if task.status == '完了' %>
                <i class="bi bi-check-circle-fill text-success me-2"></i>
              <% else %>
                <i class="bi bi-circle text-secondary me-2"></i>
              <% end %>

              <%# ✅ タイトル表示（取り消し線は不使用、完了時はグレー） %>
              <h5 class="mb-0 <%= task.status == '完了' ? 'text-muted' : '' %>">
                <%= link_to task.title, task_path(task) %>
              </h5>
            </div>

            <small><%= task.due_date.present? ? task.due_date.strftime('%Y/%m/%d') : '期限なし' %></small>
          </div>

          <%# 詳細（最大50文字まで） %>
          <p class="mb-1 ms-4 ps-2"><%= task.description.truncate(50) if task.description %></p>

          <div class="d-flex justify-content-between align-items-center mt-2">
            <%# ステータスバッジ表示 %>
            <span class="badge <%= task.status == '完了' ? 'bg-success' : 'bg-warning' %>"><%= task.status %></span>

            <%# 編集・削除ボタン %>
            <div>
              <%= link_to "編集", edit_task_path(task), class: "btn btn-sm btn-primary" %>
              <%= link_to "詳細", task_path(task), class: "btn btn-sm btn-warning" %>

            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="alert alert-info">
      タスクがありません。新しいタスクを作成しましょう。
    </div>
  <% end %>
</div>

<%# ✅ 初回表示ガイド（任意） %>
<div class="guide-tooltip d-none alert alert-info position-fixed top-0 end-0 m-4 shadow">
  <div class="d-flex align-items-center justify-content-between">
    <strong>タスクを管理できます！</strong>
    <button type="button" class="btn-close ms-3 guide-close" aria-label="閉じる"></button>
  </div>
</div>
